
# 📘 React E-Commerce Frontend Setup

*(Using Vite + React + Tailwind CSS)*

---

## 🔹 1. Create React Project using Vite

Run the following command:

```bash
npm create vite@latest react-ecommerce-app -- --template react
```

### 📌 Navigate to project folder

```bash
cd react-ecommerce-app
```

### 📌 Open project in VS Code

```bash
code .
```

---

## 🔹 2. Install Required Dependencies

```bash
npm install react-hot-toast
npm install sweetalert2
npm install react-router-dom
npm install tailwindcss @tailwindcss/vite
```

### 📌 Package Purpose

| Package          | Purpose              |
| ---------------- | -------------------- |
| react-hot-toast  | Show notifications   |
| sweetalert2      | Stylish alert popups |
| react-router-dom | Page navigation      |
| tailwindcss      | UI styling           |

---

## 🔹 3. Configure Tailwind CSS (Vite Setup)

👉 Refer official docs: Tailwind CSS

---

### 📌 Step 1: Update `vite.config.js`

```js
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import tailwindcss from '@tailwindcss/vite'

export default defineConfig({
  plugins: [react(), tailwindcss()],
})
```

---

### 📌 Step 2: Update `index.css`

```css
@import "tailwindcss";
```

---

## 🔹 4. Run the Application

```bash
npm run dev
```

👉 Application runs on:

```
http://localhost:5173
```

---

## 🔹 5. Test Tailwind Installation

Update `App.jsx`:

```jsx
function App() {
  return (
    <h1 className="text-3xl font-bold underline text-red-500">
      Hello world!
    </h1>
  );
}

export default App;
```

✅ If styling is visible → Tailwind is working correctly

---

## 🔹 6. Project Structure

```text
react-ecommerce-app/
│
├── src/
│   ├── api/
│   │   └── api.js
│   │
│   ├── assets/
│   │   ├── hero.png
│   │   ├── react.svg
│   │   └── vite.svg
│   │
│   ├── components/
│   │   └── Navbar.jsx
│   │
│   ├── context/
│   │   ├── AuthContext.jsx
│   │   └── CartContext.jsx
│   │
│   ├── pages/
│   │   ├── AdminOrders.jsx
│   │   ├── Cart.jsx
│   │   ├── Checkout.jsx
│   │   ├── Login.jsx
│   │   ├── Orders.jsx
│   │   ├── Products.jsx
│   │   └── Register.jsx
│   │
│   ├── App.jsx
│   ├── main.jsx
│   └── index.css
│
├── public/
├── package.json
├── vite.config.js
└── .env
```

---

## 🔹 7. Application Architecture

### 📌 Components

Reusable UI elements
Example: Navbar

### 📌 Pages

Each page represents a route:

* Products
* Cart
* Checkout
* Login / Register
* Orders
* Admin Orders

### 📌 Context API

* `AuthContext` → Authentication management
* `CartContext` → Cart state management

### 📌 API Layer

* `api.js` → Handles backend communication (Axios / Fetch)

---

## 🔹 8. Basic Routing Setup

```jsx
import { BrowserRouter, Routes, Route } from "react-router-dom";
import Products from "./pages/Products";
import Cart from "./pages/Cart";

function App() {
  return (
    <BrowserRouter>
      <Routes>
        <Route path="/" element={<Products />} />
        <Route path="/cart" element={<Cart />} />
      </Routes>
    </BrowserRouter>
  );
}

export default App;
```

---

## 🔹 9. Environment Configuration

Create `.env` file:

```env
VITE_API_URL=http://localhost:5000
```

### 📌 Usage in code

```js
const API_URL = import.meta.env.VITE_API_URL;
```

---

## 🔹 10. Key Features Implemented

✅ React with Vite setup
✅ Tailwind CSS integration
✅ Routing using React Router
✅ Global state using Context API
✅ Modular folder structure
✅ Ready for backend integration

---

## 🔹 11. Suggested Enhancements

You can extend this project with:

* Product listing UI
* Add to Cart functionality
* Authentication (JWT)
* Order management
* Payment integration (e.g., Razorpay)
* Admin dashboard

---

## 🔹 12. Summary

This project sets up a **scalable frontend architecture** using modern tools:

* ⚡ Fast development with Vite
* ⚛️ Component-based UI with React
* 🎨 Styling with Tailwind
* 🔁 Routing with React Router
* 🌐 API-ready structure


